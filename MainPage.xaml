<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BudgetManager4U"
             x:Class="BudgetManager4U.MainPage"
             BackgroundColor="{DynamicResource PageBackgroundColor}">
    
    <StackLayout Padding="20">
        <Label Text="Budget Planner" FontSize="24" HorizontalOptions="Center" />

        <CollectionView x:Name="BudgetItemsCollectionView" ItemsSource="{Binding BudgetItems}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10">
                        <Label Text="{Binding Date}" />
                        <Label Text="{Binding Category}" />
                        <Label Text="{Binding Description}" />
                        <Label Text="{Binding Amount}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="Load Budget" Clicked="OnLoadBudgetClicked" />
        <Button Text="Save Budget" Clicked="OnSaveBudgetClicked" />
    </StackLayout>
</ContentPage>
